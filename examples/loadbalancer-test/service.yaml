apiVersion: v1
kind: Service
metadata:
  name: echo
  namespace: lb-test
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: echo
  ports:
    - name: http
      port: 80
      targetPort: http
